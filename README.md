# TODO App V1 🎓

Минимально рабочая версия TODO приложения для обучения новичков.

## 📋 Функции

- ✅ **Добавить** задачу
- ✅ **Удалить** задачу
- ✅ **Отметить** как выполненную
- ✅ **Редактировать** текст задачи
- ✅ **Фильтрация** (Все / Активные / Выполненные)
- ✅ **Сохранение** в localStorage (данные сохраняются даже после перезагрузки)

## 🛠 Технологии

- **React 18** - UI компонент
- **Tailwind CSS** - стили
- **Vite** - сборщик
- **localStorage** - сохранение данных в браузере

## � Описание файлов для новичков

**`src/App.jsx`** — Главный компонент со всей логикой, состояния и UI в одном месте (идеально для обучения).

**`src/main.jsx`** — Точка входа приложения, монтирует React компонент в DOM.

**`src/index.css`** — Стили Tailwind CSS, подключает утилиты для стилизации UI.

**`index.html`** — HTML шаблон, контейнер для приложения (тег `<div id="root">`).

**`package.json`** — Конфигурация проекта, описывает зависимости и скрипты запуска.

**`vite.config.js`** — Конфигурация Vite, настройки сборщика и плагин React.

**`tailwind.config.js`** — Конфигурация Tailwind, пути к файлам с классами CSS.

**`postcss.config.js`** — Конфигурация PostCSS, подключает Tailwind и autoprefixer к CSS.

## �📦 Структура

```
todo-app/
├── src/
│   ├── App.jsx          # Главный компонент со всей логикой
│   ├── main.jsx         # Точка входа
│   └── index.css        # Tailwind стили
├── index.html           # HTML контейнер
├── package.json         # Зависимости
├── vite.config.js       # Конфигурация Vite
├── tailwind.config.js   # Конфигурация Tailwind
└── postcss.config.js    # PostCSS конфигурация
```

## 🚀 Как запустить

1. **Установите зависимости:**

    ```bash
    cd c:\dev\todo-app
    npm install
    ```

2. **Запустите в режиме разработки:**

    ```bash
    npm run dev
    ```

3. **Откройте браузер:**
   Перейдите на `http://localhost:5173`

## 📝 Как использовать

1. Введите текст задачи в поле "Добавьте новую задачу..."
2. Нажмите кнопку "Добавить" или Enter
3. Используйте кнопки:
    - **✅** - отметить как выполненную
    - **✎ Ред.** - редактировать текст
    - **🗑 Уд.** - удалить задачу
4. Фильтруйте по статусу: "Все", "Активные", "Выполненные"

## 💡 Для обучения

Весь код находится в одном файле `App.jsx` с подробными комментариями:

- Команды для управления состоянием
- Объяснение каждой функции
- Примеры работы с localStorage
- Условный рендеринг и списки в React

## 🎯 Что можно добавить позже

- Категории/теги для задач
- Приоритеты (высокий/средний/низкий)
- Сортировка по дате
- Поиск по тексту
- Напоминания (notifications API)
- Экспорт/импорт задач

---

**Версия:** V1 (Минимально рабочая)
**Дата создания:** 27.02.2026
